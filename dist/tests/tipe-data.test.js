"use strict";
describe("Data Type", () => {
    it("should must declare", () => {
        const name = "Annisa Fitriani Haqiqi";
        const balance = 10000000;
        const isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
});
