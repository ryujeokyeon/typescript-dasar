describe("Data Type", () => {
  it("should must declare", () => {
    const name: string = "Annisa Fitriani Haqiqi";
    const balance: number = 10000000;
    const isVip: boolean = true;

    console.info(name);
    console.info(balance);
    console.info(isVip);
  });
});
